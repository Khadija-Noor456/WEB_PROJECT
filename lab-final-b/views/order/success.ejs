<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/css/style.css">
  <title>Order Success</title>
</head>
<body>
  <div class="page-header">
    <h1>Order Placed Successfully!</h1>
  </div>

  <div class="success" style="font-size: 60px; text-align: center;">✓</div>
  <p>Order ID: <strong><%= order._id %></strong></p>
  <p>Status: <strong><%= order.status %></strong></p>

  <div class="order-summary">
    <h3>Order Items:</h3>
    <% order.items.forEach(item => { %>
      <p><%= item.productName %> (× <%= item.quantity %>) - $<%= item.totalPrice.toFixed(2) %></p>
    <% }) %>
    <hr>
    <p>Subtotal: $<%= order.subtotal.toFixed(2) %></p>
    <% if (order.discount > 0) { %>
      <p style="color: var(--accent)">Discount: -$<%= order.discount.toFixed(2) %></p>
    <% } %>
    <h3>Total: $<%= order.total.toFixed(2) %></h3>
  </div>

  <p>Confirmation sent to: <strong><%= order.email %></strong></p>

  <div class="product-actions">
    <a href="/" class="btn btn-primary">Continue Shopping</a>
    <a href="/order/my-orders" class="btn btn-secondary">View My Orders</a>
  </div>
</body>
</html>
